[buildout]
index = https://pypi.org/simple
find-links = https://${buildout:links-login}:${buildout:links-password}@1proyectos.matem.unam.mx/distributions/

download-cache = downloads
eggs-directory = eggs

extends =
    linkto/base.cfg
    local.cfg
    secret.cfg
    floating_versions_project.cfg
    pinned_versions_project.cfg

# The project name, base for paths
site = projectname

languages = en es

eggs +=
    Products.CMFPlone
    plone.api
    plone.classicui
    plone.restapi
    imatem.utils
#    project.site

test-eggs +=
#    project.site [test]

auto-checkout +=
#    project.site

zcml +=

[sources]
# project.site = fs project.site full-path=${buildout:directory}/src/project.site
